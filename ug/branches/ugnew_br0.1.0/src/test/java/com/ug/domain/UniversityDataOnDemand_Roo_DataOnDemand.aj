// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ug.domain;

import com.ug.domain.University;
import java.util.List;
import java.util.Random;
import org.springframework.stereotype.Component;

privileged aspect UniversityDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UniversityDataOnDemand: @Component;
    
    private Random UniversityDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<University> UniversityDataOnDemand.data;
    
    public University UniversityDataOnDemand.getNewTransientUniversity(int index) {
        com.ug.domain.University obj = new com.ug.domain.University();
        java.lang.String universityName = "universityName_" + index;
        if (universityName.length() > 20) {
            universityName  = universityName.substring(0, 20);
        }
        obj.setUniversityName(universityName);
        java.lang.String universityWebsite = "universityWebsite_" + index;
        if (universityWebsite.length() > 30) {
            universityWebsite  = universityWebsite.substring(0, 30);
        }
        obj.setUniversityWebsite(universityWebsite);
        obj.setPhone("phone_" + index);
        obj.setMobile("mobile_" + index);
        java.lang.String address1 = "address1_" + index;
        if (address1.length() > 50) {
            address1  = address1.substring(0, 50);
        }
        obj.setAddress1(address1);
        java.lang.String address2 = "address2_" + index;
        if (address2.length() > 50) {
            address2  = address2.substring(0, 50);
        }
        obj.setAddress2(address2);
        java.lang.String city = "city_" + index;
        if (city.length() > 50) {
            city  = city.substring(0, 50);
        }
        obj.setCity(city);
        obj.setState(com.ug.reference.State.class.getEnumConstants()[0]);
        java.lang.String zip = "zip_" + index;
        if (zip.length() > 10) {
            zip  = zip.substring(0, 10);
        }
        obj.setZip(zip);
        java.lang.String country = "country_" + index;
        if (country.length() > 20) {
            country  = country.substring(0, 20);
        }
        obj.setCountry(country);
        obj.setEnabled(Boolean.TRUE);
        obj.setLocked(Boolean.TRUE);
        return obj;
    }
    
    public University UniversityDataOnDemand.getSpecificUniversity(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        University obj = data.get(index);
        return University.findUniversity(obj.getId());
    }
    
    public University UniversityDataOnDemand.getRandomUniversity() {
        init();
        University obj = data.get(rnd.nextInt(data.size()));
        return University.findUniversity(obj.getId());
    }
    
    public boolean UniversityDataOnDemand.modifyUniversity(University obj) {
        return false;
    }
    
    public void UniversityDataOnDemand.init() {
        data = com.ug.domain.University.findUniversityEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'University' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<com.ug.domain.University>();
        for (int i = 0; i < 10; i++) {
            com.ug.domain.University obj = getNewTransientUniversity(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
