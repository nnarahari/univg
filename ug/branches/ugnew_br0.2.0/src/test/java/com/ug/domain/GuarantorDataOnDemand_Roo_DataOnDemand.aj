// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ug.domain;

import com.ug.domain.Guarantor;
import com.ug.domain.UniversityDataOnDemand;
import java.util.List;
import java.util.Random;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect GuarantorDataOnDemand_Roo_DataOnDemand {
    
    declare @type: GuarantorDataOnDemand: @Component;
    
    private Random GuarantorDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Guarantor> GuarantorDataOnDemand.data;
    
    @Autowired
    private UniversityDataOnDemand GuarantorDataOnDemand.universityDataOnDemand;
    
    public Guarantor GuarantorDataOnDemand.getNewTransientGuarantor(int index) {
        com.ug.domain.Guarantor obj = new com.ug.domain.Guarantor();
        java.lang.String firstName = "firstName_" + index;
        if (firstName.length() > 50) {
            firstName  = firstName.substring(0, 50);
        }
        obj.setFirstName(firstName);
        java.lang.String middleName = "middleName_" + index;
        if (middleName.length() > 30) {
            middleName  = middleName.substring(0, 30);
        }
        obj.setMiddleName(middleName);
        java.lang.String lastName = "lastName_" + index;
        if (lastName.length() > 50) {
            lastName  = lastName.substring(0, 50);
        }
        obj.setLastName(lastName);
        obj.setGender(com.ug.reference.Gender.class.getEnumConstants()[0]);
        java.lang.String homePage = "homePage_" + index;
        if (homePage.length() > 30) {
            homePage  = homePage.substring(0, 30);
        }
        obj.setHomePage(homePage);
        java.lang.String linkedInProfile = "linkedInProfile_" + index;
        if (linkedInProfile.length() > 75) {
            linkedInProfile  = linkedInProfile.substring(0, 75);
        }
        obj.setLinkedInProfile(linkedInProfile);
        obj.setHomePhone("homePhone_" + index);
        obj.setWorkPhone("workPhone_" + index);
        obj.setMobile("mobile_" + index);
        obj.setUniversity(universityDataOnDemand.getRandomUniversity());
        obj.setEnabled(Boolean.TRUE);
        obj.setLocked(Boolean.TRUE);
        java.lang.String address1 = "address1_" + index;
        if (address1.length() > 50) {
            address1  = address1.substring(0, 50);
        }
        obj.setAddress1(address1);
        java.lang.String address2 = "address2_" + index;
        if (address2.length() > 50) {
            address2  = address2.substring(0, 50);
        }
        obj.setAddress2(address2);
        java.lang.String city = "city_" + index;
        if (city.length() > 50) {
            city  = city.substring(0, 50);
        }
        obj.setCity(city);
        obj.setState(com.ug.reference.State.class.getEnumConstants()[0]);
        java.lang.String zip = "zip_" + index;
        if (zip.length() > 10) {
            zip  = zip.substring(0, 10);
        }
        obj.setZip(zip);
        java.lang.String country = "country_" + index;
        if (country.length() > 20) {
            country  = country.substring(0, 20);
        }
        obj.setCountry(country);
        return obj;
    }
    
    public Guarantor GuarantorDataOnDemand.getSpecificGuarantor(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Guarantor obj = data.get(index);
        return Guarantor.findGuarantor(obj.getId());
    }
    
    public Guarantor GuarantorDataOnDemand.getRandomGuarantor() {
        init();
        Guarantor obj = data.get(rnd.nextInt(data.size()));
        return Guarantor.findGuarantor(obj.getId());
    }
    
    public boolean GuarantorDataOnDemand.modifyGuarantor(Guarantor obj) {
        return false;
    }
    
    public void GuarantorDataOnDemand.init() {
        data = com.ug.domain.Guarantor.findGuarantorEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Guarantor' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<com.ug.domain.Guarantor>();
        for (int i = 0; i < 10; i++) {
            com.ug.domain.Guarantor obj = getNewTransientGuarantor(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
